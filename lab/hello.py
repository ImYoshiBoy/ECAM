#greet function ! :D
def greet(name: str) -> str:
    """Returns Hello, 'name'. Useful to greet someone."""
    print(f"Hello, {name}.") #I do not use 'return' since I only want to print something, I dont need to affect this function to a variable.
